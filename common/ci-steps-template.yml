steps:

# ==========================
# Step Group 1: Version and Setup
# ==========================

- checkout: self
  fetchDepth: 0
  persistCredentials: true
  displayName: 'Checkout Code'

- task: gitversion/setup@3.0.0
  displayName: 'Install GitVersion'
  inputs:
    versionSpec: '5.x'

- task: gitversion/execute@3.0.0
  displayName: 'Calculate Version'

- script: |
    echo "##[group]Build Information"
    echo "Version: $(GitVersion.MajorMinorPatch)"
    echo "Image: $(registryServerName)/$(imageName):$(GitVersion.MajorMinorPatch)"
    echo "Branch: $(Build.SourceBranch)"
    echo "Commit: $(Build.SourceVersion)"
    echo "##[endgroup]"
  displayName: 'Display Build Info'

# ==========================
# Step Group 2: Build Docker Image
# ==========================

- script: |
    echo "Logging in to Azure Container Registry..."
    echo $(registryPassword) | docker login $(registryServerName) -u $(registryLogin) --password-stdin
  displayName: 'Docker Login - ACR'
  condition: succeeded()

- script: |
    echo "Building Docker image..."
    docker build \
      --tag $(registryServerName)/$(imageName):$(GitVersion.MajorMinorPatch) \
      --tag $(registryServerName)/$(imageName):latest \
      .
  displayName: 'Build Docker Image'
  condition: succeeded()

# ==========================
# Step Group 3: Security Scan
# ==========================

- script: |
    echo "Running Trivy security scan..."
    docker run --rm \
      -v /var/run/docker.sock:/var/run/docker.sock \
      -v $(Agent.TempDirectory):/tmp \
      ghcr.io/aquasecurity/trivy:latest image \
      --exit-code 0 \
      --severity HIGH,CRITICAL \
      --scanners vuln,misconfig,secret \
      --ignore-unfixed \
      --format table \
      $(registryServerName)/$(imageName):$(GitVersion.MajorMinorPatch)

    echo "##[section]Running strict scan (will fail on HIGH/CRITICAL findings)"
    docker run --rm \
      -v /var/run/docker.sock:/var/run/docker.sock \
      -v $(Agent.TempDirectory):/tmp \
      ghcr.io/aquasecurity/trivy:latest image \
      --exit-code 1 \
      --severity HIGH,CRITICAL \
      --scanners vuln,misconfig,secret \
      --ignore-unfixed \
      --format json \
      --output /tmp/trivy-results.json \
      $(registryServerName)/$(imageName):$(GitVersion.MajorMinorPatch)
  displayName: 'Security Scan with Trivy'
  condition: succeeded()

- task: PublishBuildArtifacts@1
  displayName: 'Publish Trivy Results'
  condition: always()
  inputs:
    PathtoPublish: '$(Agent.TempDirectory)/trivy-results.json'
    ArtifactName: 'security-scan-results'
    publishLocation: 'Container'

# ==========================
# Step Group 4: Push Docker Image
# ==========================

- script: |
    echo "Pushing Docker image to ACR..."
    docker push $(registryServerName)/$(imageName):$(GitVersion.MajorMinorPatch)
    docker push $(registryServerName)/$(imageName):latest
  displayName: 'Push Docker Image'
  condition: and(succeeded(), eq(variables['build.sourceBranch'], 'refs/heads/master'))

# ==========================
# Step Group 5: Git Tag
# ==========================

- script: |
    git config user.email "no-reply@quanby.nl"
    git config user.name "CI Bot"

    if git rev-parse "v$(GitVersion.MajorMinorPatch)" >/dev/null 2>&1; then
      echo "Tag v$(GitVersion.MajorMinorPatch) already exists, skipping..."
    else
      echo "Creating tag v$(GitVersion.MajorMinorPatch)..."
      git tag -a "v$(GitVersion.MajorMinorPatch)" -m "Release $(GitVersion.MajorMinorPatch) [skip ci]"
      git push origin "v$(GitVersion.MajorMinorPatch)"
    fi
  displayName: 'Create Git Tag'
  condition: and(succeeded(), eq(variables['build.sourceBranch'], 'refs/heads/master'))

# ==========================
# Step Group 6: Helm Chart
# ==========================

- task: HelmInstaller@1
  displayName: 'Install Helm'
  inputs:
    helmVersionToInstall: $(helmVersion)

- script: |
    echo "Logging in to Helm OCI registry..."
    helm registry login $(registryServerName) --username $(registryLogin) --password $(registryPassword)
  displayName: 'Helm Registry Login'

- script: |
    echo "Packaging Helm chart..."
    helm package --version $(GitVersion.MajorMinorPatch) --app-version $(GitVersion.MajorMinorPatch) charts/$(projectName)
    if [ ! -f "$(projectName)-$(GitVersion.MajorMinorPatch).tgz" ]; then
        echo "Error: Helm chart package not found!"
        exit 1
    fi
  displayName: 'Package Helm Chart'

- script: |
    echo "Pushing Helm chart to ACR..."
    chartPackage=$(ls $(projectName)-$(GitVersion.MajorMinorPatch).tgz)
    helm push $chartPackage oci://$(registryName).azurecr.io/helm
    echo $(jq -n --arg version "$(GitVersion.MajorMinorPatch)" '{helmChartVersion: $version}') > $(build.artifactStagingDirectory)/variables.json
  failOnStderr: false
  condition: and(succeeded(), eq(variables['build.sourceBranch'], 'refs/heads/master'))
  displayName: 'Push Helm Chart to ACR'

- publish: $(build.artifactStagingDirectory)
  artifact: build-artifact
  condition: and(succeeded(), eq(variables['build.sourceBranch'], 'refs/heads/master'))
  displayName: 'Publish Build Artifacts'
